---
import '../styles/global.css';
import SEO from '../components/SEO.astro';
import FloatingCTA from '../components/FloatingCTA.astro';
import QuoteModal from '../components/QuoteModal.astro';
import OfferModal from '../components/OfferModal.astro';
import EnhancedTracking from '../components/EnhancedTracking.astro';
import GoogleReviewsBadge from '../components/GoogleReviewsBadge.astro';

interface Props {
  title: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  noindex?: boolean;
}

const {
  title,
  description = 'Upgrade your Canberra home with All Seasons Living — specialists in artificial lawn, custom roller shutters, outdoor blinds and plantation shutters. High-quality products, expert installers, and beautifully finished results every time. Book your free measure & quote.',
  canonical,
  ogImage,
  noindex = false
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <!-- Google Tag Manager -->
    <script is:inline>
      (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
      new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
      j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
      'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
      })(window,document,'script','dataLayer','GTM-NFH69VZZ');
    </script>
    <!-- End Google Tag Manager -->

    <SEO
      title={title}
      description={description}
      canonical={canonical}
      ogImage={ogImage}
      noindex={noindex}
    />

    <!-- Page-specific head content -->
    <slot name="head" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon.png" />

    <!-- Performance: DNS prefetch -->
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://fonts.gstatic.com" />

    <!-- Google Fonts - DM Sans - Optimized loading with font-display -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap"
      rel="stylesheet"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet" />
    </noscript>

    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-CW5WNR8Z5Y"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-CW5WNR8Z5Y');
    </script>

    <!-- Google reCAPTCHA v3 -->
    <script src="https://www.google.com/recaptcha/api.js?render=6LdAqNUrAAAAAAcglbSVCbhSGRd-kLE8fTtCbmbO" async defer></script>

    <!-- Vercel Speed Insights -->
    <script>
      import { injectSpeedInsights } from '@vercel/speed-insights';
      injectSpeedInsights();
    </script>

    <!-- Vercel Web Analytics -->
    <script>
      import { inject } from '@vercel/analytics';
      inject();
    </script>
  </head>
  <body>
    <!-- Google Tag Manager (noscript) -->
    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NFH69VZZ"
    height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
    <!-- End Google Tag Manager (noscript) -->

    <slot />
    <FloatingCTA />
    <QuoteModal />
    <OfferModal />
    <GoogleReviewsBadge />
    <EnhancedTracking />
  </body>
</html>
